const detail = require('./detail.json')
const courseDetail = require('./course_detail.json')
const select = require('./select.json')
module.exports = {
  // 'GET /_api/v1/plugin/group_buy/1': detail, // 储值卡
  // 'GET /_api/v1/plugin/group_buy/2': courseDetail, // 课程包
  // 'GET /_api/v1/cards/deposit/brand_list': select
}
