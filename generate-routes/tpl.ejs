/**
* this js file is auto generated, you should not modifyed this file
**/
<% importServiceArray.forEach(item => {%>
import { <%- item.service%> } from '<%- item.servicePath%>'<% }); %>
<% importArr.forEach(item => {%>
const <%- item.component%> = () => import('<%- item.pagePath%>')<% }); %>

// service
const pageRoutes = [<% pageRoutes.forEach((item, index) => {%>{
  name: '<%- item.name %>',
  path: '<%- item.path %>',
  component: <%- item.component %>,
  guards: [<%- item.guards %>]<% if(item.children){%>,<%};%><% if(item.children){%>
  children: [<%item.children.forEach(c1Item => {%>{
    name: '<%- c1Item.name %>',
    path: '<%- c1Item.path %>',
    component: <%- c1Item.component %>,
    guards: [<%- c1Item.guards %>]<% if(c1Item.children){%>,<%};%><% if(c1Item.children){%>
    children: [
      <%c1Item.forEach( c2Item => {%>
        name: '<%- c2Item.name %>',
        path: '<%- c2Item.path %>',
        component: <%- c2Item.component %>
        guards: [<%- c2Item.guards %>]<% if(c1Item.children){%>,<%};%><% if(c1Item.children){%>
          children: [
            <%c2Item.forEach(c3Item => {%>
              name: '<%- c3Item.name %>',
              path: '<%- c3Item.path %>',
              guards: [<%- c3Item.guards %>],
              component: <%- c3Item.component %>
            <%});%>
          ]<%};%><%})%>
    ]<%};%>
  }<%});%>
  ]<%};%>
}<%if(index !== pageRoutes.length-1){%>, <%}%><% }); %>]

export default pageRoutes
